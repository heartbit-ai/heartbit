# Anthropic API (direct)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenRouter API (supports multiple models)
# OPENROUTER_API_KEY=sk-or-...

# Provider selection: "anthropic" or "openrouter" (default: openrouter if key is set)
# HEARTBIT_PROVIDER=openrouter

# Model override (provider-specific)
# HEARTBIT_MODEL=anthropic/claude-sonnet-4
