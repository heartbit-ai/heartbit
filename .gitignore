/target
*.swp
*.swo
.env
gateway/.ag_*.log
docker-compose.override.yml

# Logs
*.log

# Secrets / certificates (never commit)
*.pem
*.key
*.p12
*.pfx
*.secret

# Local dev configs (use *.example templates)
daemon-dev.toml
daemon-crm.toml
telegram-test.toml
gateway/config.yaml

# Runtime state: persona mention/budget/post JSONL stores
.heartbit/

# Model cache (fastembed ONNX models)
.fastembed_cache/

# Git worktrees (used by superpowers workflow)
.worktrees/

# Scratch / test artifacts
orchestrated.txt
probe.txt
summary.txt
test3-hello.txt
rust_version.txt

# Claude Code local session metadata
.claude/

# Wrangler / Cloudflare Pages local cache
.wrangler/
.vercel/

# heartbit-tui cwd artifacts (/export, /analyze, /research)
heartbit-session-*.md
heartbit-trace-*.jsonl
heartbit-diagnosis-*.md
heartbit-lessons.md

research-*.md

# Agent scratch/temporary work (the "temporary directory" the prompt points at)
scratch/

# Agent-session scratch projects at the repo root (pre-scratch/-convention debris)
crm-project/
plate_solving_research/
plate_solving_rust/
scratch-rpg-game/
# wget's default log file has no .log suffix, so `*.log` above doesn't catch it
wget-log
