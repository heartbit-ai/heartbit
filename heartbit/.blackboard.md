# Blackboard - CRM Exploration

## Status: Investigating

## Findings so far:
- Found directory: `mini-crm/` at workspace root
- Rust project with multiple packages (heartbit_core, heartbit_ghost, heartbit_gateway, heartbit_sensors, heartbit_telegram, heartbit_tui, etc.)
- Build artifacts in `target/` indicate a Rust workspace
